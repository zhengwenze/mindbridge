"use client";

import { AdminOverviewPage } from "./admin-section-pages";

/** Backward-compatible entry point for existing imports. */
export function AdminDashboard() {
  return <AdminOverviewPage />;
}
